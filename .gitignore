.pytest_cache/
*.egg-info/
__pycache__/
*.py[cod]
.venv/
benchmarks/swebench/results/

# LegalBench per-task dataset files are NOT committed (CC BY 4.0; large files).
# manifest.v1.json and README.md in data/legalbench/ are tracked.
data/legalbench/*/

# Web dashboard build artifacts. Source lives in apps/web/; the built static
# export is committed at magi_agent/web_dashboard/ (see scripts/build-web-dashboard.sh).
apps/web/node_modules/
apps/web/dist/
apps/web/.next/
*.tsbuildinfo

# Local runtime state (durable evidence ledger, session logs)
.magi/
