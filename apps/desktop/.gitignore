# Rust build output.
/target
/core/target

# Tauri v2 generated schemas/context (regenerated by tauri-build).
/gen

# Built sidecar binaries and generated icons are release artifacts, not source.
/binaries/magi*
/icons/*.png
/icons/*.icns
/icons/*.ico

# Cargo lockfile policy: keep it for the app (reproducible desktop builds).
!Cargo.lock
