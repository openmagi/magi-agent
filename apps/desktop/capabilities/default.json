{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Open Magi desktop shell. Grants only the ability to open external (non-loopback) URLs in the system browser. No filesystem, shell-exec, or arbitrary command permissions are granted to the webview.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url"
  ]
}
