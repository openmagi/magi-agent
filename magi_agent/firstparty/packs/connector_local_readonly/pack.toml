# Bundled first-party connector (MCP) pack (no privilege; same loader/format).
# Schema = magi_agent/packs/manifest.py (top-level packId/displayName).

packId = "openmagi.connector-local-readonly"
displayName = "Local read-only connector"
version = "1.0.0"
description = "First-party read-only local connector bundled as a removable pack."

[[provides]]
type = "connector"
ref = "connector:local-readonly@1"
impl = "magi_agent.firstparty.packs.connector_local_readonly.impl:provide_connector"
