# Internal Notes

This directory keeps planning notes, compatibility notes, and operational
handoffs that are useful to maintainers but should not be rendered as public
OpenMagi documentation.

Public user and developer documentation lives under `docs/` and is indexed by
`docs/manifest.json`.
